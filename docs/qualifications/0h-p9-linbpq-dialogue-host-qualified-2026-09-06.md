# 0H-P9 LinBPQ dialogue host qualification

Candidate `b5960010494bc8eedad5cd0c6720708cda855063` passed dedicated GitHub Actions run `34000678364` above the host-qualified 0H-P8 checkpoint.

The state machine recognizes the independently observed BPQ SID and exact `de KJ6YWD>` prompt, prepares SP/title/body/`/EX` only in order, preserves an exact body stream across 128-byte fragments, and completes only after the command prompt returns. Rejection text, non-ASCII input, missing SID, prompt mismatch, buffer overflow, and terminal reuse fail closed.

All actions remain inert. The P8 work planner and P7 physical evidence remain frozen, and no runtime owns or imports the dialogue.

# Temporary R1 pre-CI marker note

The branch name `checkpoint/0c-p8-sustained-kiss-tnc-r1-host-qualified-preci`
was created only as a temporary immutable pointer while the R1 repair was being
reviewed. It is **not** a qualification checkpoint and must not be used as a
physical-test base.

The authoritative R1 checkpoint will be named
`checkpoint/0c-p8-sustained-kiss-tnc-r1-host-qualified` only after the R1 PR,
full framework suite, final evidence commit, and exact-head CI all pass.

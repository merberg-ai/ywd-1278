# 0C-P2 packet-correlated RSSI checkpoint note

The immutable physical-evidence checkpoint is `checkpoint/0c-p2-rssi-packet-correlation-qualified` at commit `3a81fcdf8825af1267ce03af255c5f02c2242ba6`.

The later commit that adds this note does not move that checkpoint and does not alter the frozen physical evidence. Subsequent host-only threshold/hysteresis policy work must build after the checkpoint rather than rewriting it.
